export const uploadSchema = {
  type: 'object',
  properties: {
    type: {
      type: 'string'
    },
    id: {
      type: 'string'
    },
    img: {
      type: 'form-data archive'
    }
  }
}

// response
